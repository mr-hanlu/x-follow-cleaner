{
  "name": "x-follow-cleaner",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --env-file=.env.production scripts/build.mjs",
    "build:local": "DASHBOARD_URL=http://localhost:8788/ node scripts/build.mjs",
    "test": "node scripts/test.mjs"
  }
}
